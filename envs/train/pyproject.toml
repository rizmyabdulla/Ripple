[project]
name = "ripple-train-environment"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "ripple-vc[dev,train]",
  "tensorboard>=2.18,<3",
]

[tool.uv]
package = false

[tool.uv.sources]
ripple-vc = { path = "../..", editable = true }

