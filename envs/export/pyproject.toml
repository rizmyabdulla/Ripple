[project]
name = "ripple-export-environment"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "ripple-vc[dev,torch,export]",
]

[tool.uv]
package = false

[tool.uv.sources]
ripple-vc = { path = "../..", editable = true }
